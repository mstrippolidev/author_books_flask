apiVersion: v1
kind: Service
metadata:
  name: cluster-db-ha
  labels:
    application: patroni
    cluster-name: cluster-db-ha
spec:
  clusterIP: None
  selector:
    application: patroni
    cluster-name: cluster-db-ha
  ports:
    - name: pg
      port: 5432
      targetPort: 5432
    - name: api
      port: 8008
      targetPort: 8008